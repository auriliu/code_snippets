import express from "express";
const app = express();

// express is used to build an API.
// build one.
// handle all possible errors that may come your way.

app.listen(9000, () => console.log(`the server is up at port 9000`));
